# Manuscript

`main.tex` contains the proof of the certified project claim
`c_E > 0.38055925`.

Build with a standard LaTeX installation:

```bash
pdflatex -interaction=nonstopmode -halt-on-error main.tex
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

The theorem depends on the retained center certificate and the cited
noncentral certificate. Verification commands and exact machine-readable
outputs are maintained at the repository root.
